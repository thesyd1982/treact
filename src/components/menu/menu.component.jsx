import React from 'react';
import SubMenu from '../submenu/submenu.component';

const Menu = ({submenus}) => {
    return (  
        // submenus.map((subm,index) => 
        <SubMenu />
        // subm={subm} key={index} />
        // )
    );
}

export default Menu;